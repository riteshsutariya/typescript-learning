interface User {
  name: string;
  age: number;
  gender: string;
  address: string;
}

const user: User = {
  name: "ritesh",
  age: 23,
  gender: "Male",
  address: "surat city(EAST)",
};

let msg = "Message From TypeScript";

export {};
